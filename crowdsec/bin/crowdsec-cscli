#!/usr/bin/env bash
set -euo pipefail
if docker compose version >/dev/null 2>&1; then
  exec docker compose -f "${INSTALL_DIR:-/opt/crowdsec}/docker-compose.yml" exec crowdsec cscli "$@"
fi
exec docker-compose -f "${INSTALL_DIR:-/opt/crowdsec}/docker-compose.yml" exec crowdsec cscli "$@"
